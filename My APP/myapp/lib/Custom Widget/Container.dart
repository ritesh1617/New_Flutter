import 'package:myapp/const/export.dart';

Widget loginContainer({Color? color,}){
  return Container(
    decoration: BoxDecoration(
      borderRadius: BorderRadius.circular(20),
      color: color, 
    ),
  );
}